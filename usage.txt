usage: hip <action> [<args>]

Hip automatically generates the boilerplate code for creating new projects that
use React, Typescript, ESLint, Prettier and Styled Components. The currently
supported actions are:

    init    Creates a new project, requires a name to be provided.
    format  Runs the format script, using Typescript to check types and ESLint
            and Prettier to lint and format code.